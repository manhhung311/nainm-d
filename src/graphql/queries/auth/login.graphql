query Query($input: UserLoginInput!) {
  login(input: $input) {
    token
    user {
      id
      email
      firstName
      lastName
      phoneNumber
      role
      createdAt
      updatedAt
    }
  }
}
