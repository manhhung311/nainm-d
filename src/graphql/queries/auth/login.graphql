query Query($input: UserLoginInput!) {
  login(input: $input) {
    token
    user {
      id
      email
      firstName
      lastName
      fullName
      phoneNumber
      role
      createdAt
      updatedAt
    }
  }
}
